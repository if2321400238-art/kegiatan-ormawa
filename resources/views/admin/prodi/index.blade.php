<x-app-layout><x-slot name="title">Kelola Program Studi</x-slot>
<div class="page-header"><div class="page-header-main"><div class="page-header-title"><h2 class="text-lg font-semibold">Daftar Program Studi</h2><p class="text-[12px] text-gray-500">Kelola prodi, fakultas, dan penugasan Kaprodi</p></div><a href="{{ route('admin.prodi.create') }}" class="px-4 py-2 bg-brand text-white rounded-lg text-[13px]"><i class="ti ti-plus"></i> Tambah Prodi</a></div></div>
@if(session('success'))<div class="mb-4 p-3 bg-success-light text-success rounded-lg text-sm">{{ session('success') }}</div>@endif
<div class="table-card"><form class="p-4 border-b flex gap-2"><input name="search" value="{{ request('search') }}" class="flex-1 rounded-lg border-gray-200 text-[13px]" placeholder="Cari nama atau kode prodi"><button class="px-4 bg-gray-900 text-white rounded-lg text-[13px]">Cari</button></form><div class="overflow-x-auto"><table><thead><tr><th>Kode</th><th>Program Studi</th><th>Fakultas</th><th>Kaprodi</th><th>Ormawa</th><th>Aksi</th></tr></thead><tbody>@foreach($programStudi as $item)<tr><td><span class="badge badge-info">{{ $item->kode }}</span></td><td class="font-semibold">{{ $item->nama }}</td><td>{{ $item->fakultas?->nama ?? '-' }}</td><td>{{ $item->kaprodi?->nama ?? 'Belum ditentukan' }}</td><td>{{ $item->ormawas_count }}</td><td><div class="flex gap-2"><a href="{{ route('admin.prodi.edit',$item) }}" class="p-1.5 bg-warning-light text-warning rounded"><i class="ti ti-edit"></i></a>@unless($item->is_lainnya)<form method="POST" action="{{ route('admin.prodi.destroy',$item) }}" onsubmit="return confirm('Hapus program studi ini?')">@csrf @method('DELETE')<button class="p-1.5 bg-danger-light text-danger rounded"><i class="ti ti-trash"></i></button></form>@endunless</div></td></tr>@endforeach</tbody></table></div><div class="p-4 border-t">{{ $programStudi->links() }}</div></div></x-app-layout>
